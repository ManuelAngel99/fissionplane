[package]
name = "artifact-store"
description = "The one library permitted to read or write artifact bytes: manifest format, source-map sidecar, chunked ranged reads, integrity checking, and the node cache. Linked into vm-host, template-builder, and control-plane — a library plus an object bucket, not a service (docs/components/artifact-store.md)."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
thiserror = { workspace = true }

[lints]
workspace = true
